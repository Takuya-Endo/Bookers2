<h1>User info</h1>
<h1>New book</h1>
<!--上記は部分テンプレート-->

<h1>Books</h1>
<% @books.each do |book| %>
  <p>
    <%= attachment_image_tag book.user_id, :profile_image, :fill, 60, 60, format: 'jpg' 'png', fallback: "no_image.jpg" %>
    <%= link_to book.title, book_path(book.id) %>
    <%= book.body %>
  </p>
<% end %>